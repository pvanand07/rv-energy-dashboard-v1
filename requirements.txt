# RV Energy Intelligence v2.1 — Dependencies
# Install: pip install -r requirements.txt
# Run:     uvicorn main:app --reload --port 5000

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.111.0          # ASGI web framework (replaces Flask)
uvicorn[standard]==0.29.0 # ASGI server

# ── Template engine ────────────────────────────────────────────────────────
jinja2==3.1.4             # HTML templating (same as Flask's engine)
python-multipart==0.0.9   # Form data parsing (required by FastAPI)

# ── Database ───────────────────────────────────────────────────────────────
aiosqlite==0.20.0         # Async SQLite driver

# ── Data validation ────────────────────────────────────────────────────────
pydantic==2.7.1           # Request/response schema validation (built into FastAPI)

# ── Dev utilities (optional) ───────────────────────────────────────────────
httpx==0.27.0             # Async HTTP client (used in tests)
pytest==8.2.0             # Test runner
pytest-asyncio==0.23.6    # Async test support
